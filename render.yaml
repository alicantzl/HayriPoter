services:
  - type: web
    name: sync-video-site-backend
    runtime: node
    buildCommand: npm install
    startCommand: node sync-video-site/server.js
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: CORS_ORIGIN
        value: "*"
